import { Outlet } from "react-router-dom";

const Root = () => {
  return (
    <main>
      Hi asahs
      {/* <Outlet /> */}
      <button className="ct_langmode_btn">asas </button>
    </main>
  );
};

export default Root;
